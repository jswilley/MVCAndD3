(function (root) {
    //debugger;
    // Register with require these 3rd party libs 
    // which are now in the root global namespace
    //define('jquery', function () { return root.jQuery; });
    //define('d3', function () { return root.d3; });
    //define('lodash', function () { return root.lodash; });
    //define('amplify', function () { return root.amplify; });
    //define('legend', function () { return root.d3Legend; })
    
})(window);